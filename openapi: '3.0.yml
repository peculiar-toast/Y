openapi: "3.0.3"
info:
  title: "Y social API"
  version: "1.0"
servers:
  - url: http://localhost:8080/api
paths:
  /users:
    get:
      responses:
        "200":
          description: OK
  /test:
    get:
      responses:
        "200":
          description: OK
